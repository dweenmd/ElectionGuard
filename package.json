{
  "name": "gonovote-dapp",
  "version": "1.0.0",
  "description": "Blockchain-based voting system (final year project) - Solidity + Hardhat",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  }
}
